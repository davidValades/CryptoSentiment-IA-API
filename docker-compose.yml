version: "3.8"

services:
  oracle-api:
    build: .
    container_name: crypto_oracle_prod
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./oracle_memory.db:/app/oracle_memory.db
      - ./sources.json:/app/sources.json
    restart: always
